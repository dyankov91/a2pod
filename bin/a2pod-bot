#!/usr/bin/env python3
"""a2pod-bot — Telegram bot for article-to-audio conversion."""

import sys
from pathlib import Path

# Add lib to path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))

from telegram_bot import run_bot

if __name__ == "__main__":
    run_bot()
